# Zapier-style Examples (demo tokens, redacted)

- **Trigger:** Catch Hook → use your Zapier-style catch URL as the subscription target.
- **Event:** `reseller.referral_created`
- **Auth:** demo key header `X-API-Key: sk_demo_xxx` (demo only by default)

Sample payload (redacted):
```json
{ "event":"reseller.referral_created", "data":{ "referralIdSafe":"rf_***55" } }
```
