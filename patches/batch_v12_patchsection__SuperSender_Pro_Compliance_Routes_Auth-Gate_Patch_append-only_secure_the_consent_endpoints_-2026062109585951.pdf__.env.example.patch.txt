# Source: SuperSender Pro — Compliance Routes Auth-Gate Patch (append-only, secure the consent endpoints)-2026062109585951.pdf
# Target: .env.example
# Extracted PATCH section. Apply manually as append-only hook if needed.

PATCH — .env.example (append-only, placeholders only)
  # BEGIN COMPLIANCE AUTH GATE HOOK
  # Gate the /compliance dashboard page behind admin auth too (recommended).
  COMPLIANCE_GATE_DASHBOARD=true
  # Note: the API guard forces admin login for compliance mutations + PII reads

  # regardless of ADMIN_AUTH_REQUIRE_LOGIN, so set a real ADMIN_AUTH_ADMIN_PASSWORD_HASH
  # before relying on it. See the Admin Auth patch.
  # END COMPLIANCE AUTH GATE HOOK
