// lib/basketAnalysis/engine.js — co-purchase support/confidence/lift.
function round(n, dp = 3) { const f = Math.pow(10, dp); return Math.round((Number(n) || 0) * f) / f; }
function analyze(baskets, opts = {}) { const minSupportCount = opts.minSupportCount || 2; const total = baskets.length; const itemCount = new Map(); const pairCount = new Map(); for (const raw of baskets) { const items = Array.from(new Set((raw || []).filter(Boolean))); for (const it of items) itemCount.set(it, (itemCount.get(it) || 0) + 1); for (let i = 0; i < items.length; i++) for (let j = i + 1; j < items.length; j++) { const [a, b] = [items[i], items[j]].sort(); const key = a + '' + b; pairCount.set(key, (pairCount.get(key) || 0) + 1); } } const pairs = []; for (const [key, both] of pairCount.entries()) { if (both < minSupportCount) continue; const [a, b] = key.split(''); const ca = itemCount.get(a) || 0, cb = itemCount.get(b) || 0; const support = total ? both / total : 0; const confAB = ca ? both / ca : 0, confBA = cb ? both / cb : 0; const supportB = total ? cb / total : 0; const lift = supportB > 0 ? confAB / supportB : 0; pairs.push({ a, b, bothCount: both, support: round(support), confidenceAtoB: round(confAB), confidenceBtoA: round(confBA), lift: round(lift), bestDirection: confAB >= confBA ? `${a} \u2192 ${b}` : `${b} \u2192 ${a}`, bestConfidence: round(Math.max(confAB, confBA)) }); } pairs.sort((x, y) => y.lift - x.lift || y.bothCount - x.bothCount); const recsByProduct = {}; for (const p of pairs) { if (p.lift <= 1) continue; (recsByProduct[p.a] = recsByProduct[p.a] || []).push({ product: p.b, confidence: p.confidenceAtoB, lift: p.lift }); (recsByProduct[p.b] = recsByProduct[p.b] || []).push({ product: p.a, confidence: p.confidenceBtoA, lift: p.lift }); } for (const k of Object.keys(recsByProduct)) { recsByProduct[k].sort((x, y) => y.confidence - x.confidence).splice(5); } return { summary: { baskets: total, multiItemBaskets: baskets.filter((b) => new Set((b || []).filter(Boolean)).size > 1).length, products: itemCount.size, pairs: pairs.length, strongPairs: pairs.filter((p) => p.lift > 1).length }, pairs, recommendations: recsByProduct }; }
module.exports = { analyze, round };
