// lib/anomalies/detector.js — robust z-score (median + MAD).
function median(arr) { if (!arr.length) return 0; const s = arr.slice().sort((a, b) => a - b); const m = Math.floor(s.length / 2); return s.length % 2 ? s[m] : (s[m - 1] + s[m]) / 2; }
function mad(arr, med) { if (!arr.length) return 0; const m = med != null ? med : median(arr); return 1.4826 * median(arr.map((x) => Math.abs(x - m))); }
function round(n, dp = 2) { const f = Math.pow(10, dp); return Math.round((Number(n) || 0) * f) / f; }
function scoreLatest(series, opts = {}) { const window = opts.window || 30; const minHistory = opts.minHistory || 10; if (series.length < minHistory + 1) return null; const latest = series[series.length - 1]; const baseline = series.slice(Math.max(0, series.length - 1 - window), series.length - 1).map((p) => p.value); if (baseline.length < minHistory) return null; const med = median(baseline); let scale = mad(baseline, med); if (scale === 0) scale = Math.max(1e-9, Math.abs(med) * 0.1 || 1); const z = (latest.value - med) / scale; return { date: latest.date, value: round(latest.value), baselineMedian: round(med), z: round(z, 2), direction: z >= 0 ? 'spike' : 'drop', deltaPct: med !== 0 ? round(((latest.value - med) / Math.abs(med)) * 100) : null }; }
module.exports = { scoreLatest, median, mad, round };
