// lib/experiments/statistics.js — two-proportion z-test, no deps.
function normalCdf(z) { const t = 1 / (1 + 0.2316419 * Math.abs(z)); const d = 0.3989423 * Math.exp(-z * z / 2); let p = d * t * (0.3193815 + t * (-0.3565638 + t * (1.781478 + t * (-1.821256 + t * 1.330274)))); if (z > 0) p = 1 - p; return p; }
function round(n, dp = 4) { const f = Math.pow(10, dp); return Math.round((Number(n) || 0) * f) / f; }
function twoProportionTest(aConv, aN, bConv, bN, confidence = 0.95) { if (aN <= 0 || bN <= 0) return { ok: false, reason: 'insufficient sample', pA: 0, pB: 0 }; const pA = aConv / aN, pB = bConv / bN; const pPool = (aConv + bConv) / (aN + bN); const se = Math.sqrt(pPool * (1 - pPool) * (1 / aN + 1 / bN)); if (se === 0) return { ok: true, pA: round(pA), pB: round(pB), z: 0, pValue: 1, significant: false, absLift: 0, relLiftPct: 0 }; const z = (pB - pA) / se; const pValue = 2 * (1 - normalCdf(Math.abs(z))); const alpha = 1 - confidence; return { ok: true, pA: round(pA), pB: round(pB), z: round(z, 3), pValue: round(pValue), significant: pValue < alpha, confidence, absLift: round(pB - pA), relLiftPct: pA > 0 ? round(((pB - pA) / pA) * 100, 2) : null }; }
function sampleSizePerVariant(baselineRate, mde) { const p = Math.min(0.99, Math.max(0.01, baselineRate || 0.1)); const delta = Math.max(0.005, mde || 0.05); const n = Math.pow(1.96 + 0.84, 2) * 2 * p * (1 - p) / (delta * delta); return Math.ceil(n); }
module.exports = { twoProportionTest, sampleSizePerVariant, normalCdf, round };
