'use strict';
const cfg=require('./config'); const { maskMessage }=require('./redactor');
function scoreCopy(text){ const s=String(text||''); let score=50; const notes=[]; if(s.length>0&&s.length<=160) score+=15; else if(s.length>400){ score-=15; notes.push('too long'); } if(/\?$/.test(s.trim())){ score+=5; notes.push('ends with question (engaging)'); } if(/[A-Z]{5,}/.test(s)){ score-=10; notes.push('excess caps'); } if(/free|winner|urgent|guarantee/i.test(s)){ score-=15; notes.push('spam-trigger words'); } if(/\b(aap|ap|tum|آپ)\b/i.test(s)){ score+=5; notes.push('personal tone'); } return { scorePreview:Math.max(0,Math.min(100,score)), notesPreview:notes }; }
function copyPerformance(input){ const i=input||{}; const variants=Array.isArray(i.copies)?i.copies:[i.copy||'']; const resultsPreview=variants.map(c=>Object.assign({copyPreview:maskMessage(c)},scoreCopy(c))); const best=resultsPreview.slice().sort((a,b)=>b.scorePreview-a.scorePreview)[0]||null; return cfg.base({ resultsPreview, bestCopyPreview:best }); }
module.exports={ copyPerformance, scoreCopy };
