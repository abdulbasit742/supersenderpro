const engine = require('./engine');
let storeCRM = null; try { storeCRM = require('../storeCRM'); } catch {}
function collectInteractions(storeId) { if (!storeCRM) return []; const customers = storeCRM.getAllCustomers(storeId) || []; const all = []; for (const c of customers) { if (!c.phone) continue; const list = storeCRM.getCustomerInteractions(storeId, c.phone, 500) || []; for (const i of list) if (i && i.ts) all.push({ ts: i.ts, type: i.type }); } return all; }
function buildSnapshot(storeId = 'default_store', now = Date.now()) { const interactions = collectInteractions(storeId); const r = engine.analyze(interactions); const recommendations = r.topWindows.map((w) => ({ label: `${w.day} ${engine.hourLabel(w.hour)}`, day: w.day, hour: w.hour, sharePct: w.sharePct, text: `${w.day} around ${engine.hourLabel(w.hour)} — ${w.sharePct}% of customer activity happens here.` })); return { storeId, generatedAt: new Date(now).toISOString(), timeZone: r.timeZone, summary: { totalEvents: r.totalEvents, peakDay: r.peakDay, peakHour: r.peakHour == null ? null : engine.hourLabel(r.peakHour), bestWindow: recommendations[0] ? recommendations[0].label : null }, grid: r.grid, byDay: r.byDay, recommendations }; }
function buildAllSnapshot(now = Date.now()) { let ids = ['default_store']; try { ids = require('../analyticsInsights/dataSources').listStoreIds(); } catch {} const stores = ids.map((id) => buildSnapshot(id, now)); return { generatedAt: new Date(now).toISOString(), stores: ids, perStore: stores, primary: stores.find((s) => s.storeId === 'default_store') || stores[0] || null }; }
module.exports = { buildSnapshot, buildAllSnapshot, collectInteractions };
