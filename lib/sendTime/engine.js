// lib/sendTime/engine.js — hour x weekday engagement binning (tz-aware).
const DOW = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
function round(n, dp = 2) { const f = Math.pow(10, dp); return Math.round((Number(n) || 0) * f) / f; }
function localParts(ts, timeZone) { const d = new Date(ts); try { const fmt = new Intl.DateTimeFormat('en-US', { timeZone, weekday: 'short', hour: 'numeric', hour12: false }); const parts = fmt.formatToParts(d); const wd = parts.find((p) => p.type === 'weekday'); let hourStr = (parts.find((p) => p.type === 'hour') || {}).value || '0'; let hour = parseInt(hourStr, 10) % 24; const dow = Math.max(0, DOW.indexOf(wd ? wd.value : 'Sun')); return { dow, hour }; } catch { return { dow: d.getUTCDay(), hour: d.getUTCHours() }; } }
const ENGAGE_TYPES = new Set(['inbound', 'reply', 'message_in', 'order', 'loyalty_redeem']);
function analyze(interactions, opts = {}) { const timeZone = opts.timeZone || process.env.SENDTIME_TZ || 'Asia/Karachi'; const grid = Array.from({ length: 7 }, () => new Array(24).fill(0)); let total = 0; for (const i of interactions) { if (!i || !i.ts) continue; if (!ENGAGE_TYPES.has(i.type)) continue; const { dow, hour } = localParts(i.ts, timeZone); grid[dow][hour] += 1; total += 1; } const windows = []; for (let d = 0; d < 7; d++) for (let h = 0; h < 24; h++) if (grid[d][h] > 0) windows.push({ dow: d, day: DOW[d], hour: h, count: grid[d][h], sharePct: total ? round((grid[d][h] / total) * 100) : 0 }); windows.sort((a, b) => b.count - a.count); const byHour = new Array(24).fill(0), byDay = new Array(7).fill(0); for (let d = 0; d < 7; d++) for (let h = 0; h < 24; h++) { byHour[h] += grid[d][h]; byDay[d] += grid[d][h]; } const peakHour = byHour.indexOf(Math.max(...byHour)); const peakDay = byDay.indexOf(Math.max(...byDay)); return { timeZone, totalEvents: total, grid, topWindows: windows.slice(0, 5), peakHour: total ? peakHour : null, peakDay: total ? DOW[peakDay] : null, byHour, byDay: byDay.map((c, i) => ({ day: DOW[i], count: c })) }; }
function hourLabel(h) { const ampm = h < 12 ? 'AM' : 'PM'; const hr = h % 12 === 0 ? 12 : h % 12; return `${hr} ${ampm}`; }
module.exports = { analyze, hourLabel, DOW, round };
