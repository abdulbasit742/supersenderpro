// lib/insightsDigest/index.js — roll the adapters into one digest + narrative + actions.
const adapters = require('./adapters');
function money(n) { return 'PKR ' + Number(n || 0).toLocaleString(); }
function buildActions(d) { const actions = []; if (d.alerts.available && d.alerts.critical > 0) actions.push({ priority: 'critical', text: `${d.alerts.critical} critical alert(s) — check the Alerts page first.` }); if (d.churn.available && d.churn.highRisk > 0) actions.push({ priority: 'high', text: `${d.churn.highRisk} customers at high churn risk (${money(d.churn.revenueAtRisk)} at risk). Run a win-back.` }); if (d.reEngagement.available && d.reEngagement.targetedToday > 0) actions.push({ priority: 'high', text: `${d.reEngagement.targetedToday} win-back targets ready (${money(d.reEngagement.revenueAtRiskTargeted)}). Review & send.` }); if (d.concentration.available && d.concentration.risk === 'high') actions.push({ priority: 'high', text: `Revenue concentration HIGH — top customer is ${d.concentration.singleBuyerExposurePct}%. Diversify.` }); if (d.experiments.available && d.experiments.decided > 0) actions.push({ priority: 'medium', text: `${d.experiments.decided} A/B test(s) have a winner — promote the winning message.` }); if (d.cohorts.available && d.cohorts.m1RetentionTrendPct != null && d.cohorts.m1RetentionTrendPct < 0) actions.push({ priority: 'medium', text: `Newer cohorts retain ${Math.abs(d.cohorts.m1RetentionTrendPct)}% worse — look at onboarding.` }); if (d.alerts.available && d.alerts.warning > 0) actions.push({ priority: 'medium', text: `${d.alerts.warning} warning alert(s) worth a look.` }); if (!actions.length) actions.push({ priority: 'low', text: 'Nothing urgent. Numbers look steady — keep shipping.' }); return actions; }
function buildNarrative(d) { const bits = []; if (d.analytics.available) { const g = d.analytics.wowGrowthPct; bits.push(`Revenue ${money(d.analytics.revenue)} lifetime` + (g != null ? `, ${g >= 0 ? 'up' : 'down'} ${Math.abs(g)}% WoW` : '') + `.`); } if (d.forecast.available) bits.push(`Next 30 days projected ${money(d.forecast.next30Revenue)} (${money(d.forecast.next30Low)}–${money(d.forecast.next30High)})` + (d.forecast.projectedGrowthPct != null ? `, ${d.forecast.projectedGrowthPct >= 0 ? '+' : ''}${d.forecast.projectedGrowthPct}% vs last 30d` : '') + `.`); if (d.churn.available) bits.push(`Churn risk ${d.churn.predictedChurnRatePct}% with ${money(d.churn.revenueAtRisk)} at risk.`); if (d.attribution.available && d.attribution.topOpener && d.attribution.topCloser) bits.push(`${d.attribution.topOpener} opens deals, ${d.attribution.topCloser} closes them.`); return bits.join(' ') || 'Not enough data yet — run the analytics batches on PC #2.'; }
function buildDigest(storeId = 'default_store', now = Date.now()) { const sections = { analytics: adapters.analytics(storeId), forecast: adapters.forecast(storeId), churn: adapters.churn(storeId), reEngagement: adapters.reEngagement(storeId), experiments: adapters.experiments(storeId), attribution: adapters.attribution(storeId), cohorts: adapters.cohorts(storeId), alerts: adapters.alerts(storeId), clv: adapters.clv(storeId), concentration: adapters.concentration(storeId), engagement: adapters.engagement(storeId) }; const availableCount = Object.values(sections).filter((s) => s.available).length; return { storeId, generatedAt: new Date(now).toISOString(), modulesAvailable: availableCount, modulesTotal: Object.keys(sections).length, headline: { revenue: sections.analytics.available ? sections.analytics.revenue : null, mrr: sections.analytics.available ? sections.analytics.mrr : null, next30Revenue: sections.forecast.available ? sections.forecast.next30Revenue : null, revenueAtRisk: sections.churn.available ? sections.churn.revenueAtRisk : null, openAlerts: sections.alerts.available ? sections.alerts.open : null }, narrative: buildNarrative(sections), actions: buildActions(sections), sections }; }
function buildAllDigest(now = Date.now()) { let ids = ['default_store']; try { ids = require('../analyticsInsights/dataSources').listStoreIds(); } catch {} const stores = ids.map((id) => buildDigest(id, now)); return { generatedAt: new Date(now).toISOString(), stores: ids, perStore: stores, primary: stores.find((s) => s.storeId === 'default_store') || stores[0] || null }; }
module.exports = { buildDigest, buildAllDigest };
