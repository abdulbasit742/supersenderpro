// lib/cohorts/cohortEngine.js — cohort retention + LTV by acquisition month.
const MONTH_KEYS = (ts) => { const d = new Date(ts); return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, '0')}`; };
function monthDiff(fromKey, toKey) { const [fy, fm] = fromKey.split('-').map(Number); const [ty, tm] = toKey.split('-').map(Number); return (ty - fy) * 12 + (tm - fm); }
function round(n, dp = 2) { const f = Math.pow(10, dp); return Math.round((Number(n) || 0) * f) / f; }
function buildCohorts(orders, now = Date.now()) { const byPhone = {}; for (const o of orders) { if (!o.phone || !o.ts) continue; (byPhone[o.phone] = byPhone[o.phone] || []).push(o); } for (const phone of Object.keys(byPhone)) byPhone[phone].sort((a, b) => new Date(a.ts).getTime() - new Date(b.ts).getTime()); const cohortMembers = {}; for (const [phone, list] of Object.entries(byPhone)) { const cohortKey = MONTH_KEYS(list[0].ts); (cohortMembers[cohortKey] = cohortMembers[cohortKey] || new Set()).add(phone); } const nowKey = MONTH_KEYS(now); const cohortKeys = Object.keys(cohortMembers).sort(); const maxMonthsTracked = cohortKeys.length ? monthDiff(cohortKeys[0], nowKey) : 0; const cohorts = cohortKeys.map((cohortKey) => { const members = cohortMembers[cohortKey]; const size = members.size; const monthsSince = Math.max(0, monthDiff(cohortKey, nowKey)); const activeByMonth = {}, revenueByMonth = {}; for (const phone of members) { for (const o of byPhone[phone]) { const m = monthDiff(cohortKey, MONTH_KEYS(o.ts)); if (m < 0) continue; (activeByMonth[m] = activeByMonth[m] || new Set()).add(phone); revenueByMonth[m] = (revenueByMonth[m] || 0) + Number(o.amount || 0); } } const retention = [], ltv = []; let cum = 0; for (let m = 0; m <= monthsSince; m++) { const ac = activeByMonth[m] ? activeByMonth[m].size : 0; retention.push({ month: m, active: ac, pct: size ? round((ac / size) * 100) : 0 }); cum += revenueByMonth[m] || 0; ltv.push({ month: m, cumRevenue: round(cum), perMember: size ? round(cum / size) : 0 }); } const totalRevenue = round(Object.values(revenueByMonth).reduce((a, b) => a + b, 0)); return { cohort: cohortKey, size, monthsTracked: monthsSince, retention, ltv, totalRevenue, ltvPerMember: size ? round(totalRevenue / size) : 0, m1RetentionPct: retention[1] ? retention[1].pct : null, m3RetentionPct: retention[3] ? retention[3].pct : null }; }); return { cohorts, maxMonthsTracked, nowKey }; }
module.exports = { buildCohorts, MONTH_KEYS, monthDiff, round };
