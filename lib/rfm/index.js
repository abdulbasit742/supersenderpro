const engine = require('./engine');
let storeCRM = null; try { storeCRM = require('../storeCRM'); } catch {}
function mask(phone) { const s = String(phone || ''); return s.length > 5 ? s.slice(0, 4) + '****' + s.slice(-2) : '****'; }
function buildSnapshot(storeId = 'default_store', now = Date.now()) { const customers = storeCRM ? (storeCRM.getAllCustomers(storeId) || []) : []; const r = engine.analyze(customers, { now }); return { storeId, generatedAt: new Date(now).toISOString(), summary: r.summary, segments: r.segments }; }
function segmentMembers(storeId = 'default_store', segment, limit = 200, now = Date.now()) { const customers = storeCRM ? (storeCRM.getAllCustomers(storeId) || []) : []; const r = engine.analyze(customers, { now }); return r.scored.filter((s) => s.segment === segment).sort((a, b) => b.monetary - a.monetary).slice(0, limit).map((s) => ({ phone: mask(s.phone), name: s.name, rfm: s.rfm, recencyDays: Math.round(s.recencyDays), frequency: s.frequency, monetary: engine.round(s.monetary) })); }
function buildAllSnapshot(now = Date.now()) { let ids = ['default_store']; try { ids = require('../analyticsInsights/dataSources').listStoreIds(); } catch {} const stores = ids.map((id) => buildSnapshot(id, now)); return { generatedAt: new Date(now).toISOString(), stores: ids, perStore: stores, primary: stores.find((s) => s.storeId === 'default_store') || stores[0] || null }; }
module.exports = { buildSnapshot, buildAllSnapshot, segmentMembers };
