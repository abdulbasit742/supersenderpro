'use strict';
const VALID_STATUS = ['draft','in_progress','on_hold','awaiting_parts','completed','cancelled'];
function validatePart(p) { const errors = []; if (!p || typeof p !== 'object') return ['part must be an object']; if (!p.sku && !p.name) errors.push('part needs sku or name'); if (p.qty != null && (typeof p.qty !== 'number' || p.qty <= 0)) errors.push('part qty must be > 0'); if (p.unitCost != null && (typeof p.unitCost !== 'number' || p.unitCost < 0)) errors.push('part unitCost must be >= 0'); return errors; }
function validate(input) { const errors = []; if (!input || typeof input !== 'object') return { ok:false, errors:['payload must be an object'] }; if (!input.workOrderId) errors.push('workOrderId required'); if (input.status && !VALID_STATUS.includes(input.status)) errors.push('invalid status: ' + input.status); if (input.laborHours != null && (typeof input.laborHours !== 'number' || input.laborHours < 0)) errors.push('laborHours must be a non-negative number'); if (Array.isArray(input.parts)) input.parts.forEach((p,i) => validatePart(p).forEach((e) => errors.push('parts[' + i + ']: ' + e))); return { ok: errors.length === 0, errors }; }
function normalize(input) { return { workOrderId: input.workOrderId, techId: input.techId || null, status: VALID_STATUS.includes(input.status) ? input.status : 'draft', diagnosis: input.diagnosis || '', laborHours: typeof input.laborHours === 'number' ? input.laborHours : 0, parts: Array.isArray(input.parts) ? input.parts.map((p) => ({ sku:p.sku || null, name:p.name || p.sku || 'unknown part', qty:typeof p.qty === 'number' ? p.qty : 1, unitCost:typeof p.unitCost === 'number' ? p.unitCost : 0 })) : [], notes: input.notes || '' }; }
function partsCost(jobCard) { return !jobCard || !Array.isArray(jobCard.parts) ? 0 : jobCard.parts.reduce((sum,p) => sum + (p.qty || 0) * (p.unitCost || 0), 0); }
module.exports = { VALID_STATUS, validate, normalize, partsCost, validatePart };
